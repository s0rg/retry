linters-settings:
  gocritic:
    enabled-tags:
      - performance
      - diagnostic
      - style
      - opinionated

linters:
  enable-all: true
  disable:
    - gofumpt
    - varnamelen
    - testpackage
    - exhaustruct
    - nonamedreturns
    - exhaustivestruct
  fast: false

output:
  format: 'colored-line-number'
  print-issued-lines: true
  print-linter-name: true
